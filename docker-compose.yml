services:
  api:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      HOST: 0.0.0.0
      LOCAL_CONTAINER_BOOTSTRAP: "true"
      DATA_DIR: /app/data
    env_file:
      - .env.example
    volumes:
      - runtime-state:/app/data

volumes:
  runtime-state:
